import { AppShell } from '@/components/app-shell';

// The signed-out landing (design page map: /welcome). Pure presentation over the resolved tenant.
export default function WelcomePage(): React.ReactElement {
  return <AppShell />;
}
