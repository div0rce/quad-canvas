<!-- TEMPLATE: copy to docs/adr/<NNNN>-<slug>.md. Reusable ADR. -->
# ADR-`<NNNN>` — `<title>`

> Reminders: an ADR records an **architectural decision**; affected docs/contracts update in the **same PR**. Tenant-neutral (Rutgers Quad = tenant #1 example). Versions → `docs/TECH_BASELINE.md`.

- **Status:** `<proposed | accepted | superseded by ADR-#### | deprecated>`
- **Date:** `<YYYY-MM-DD>` · **Deciders:** `<roles>` · **Linked docs:** `<linked-docs>`

## 1. Context
`<the forces, constraints, and problem prompting the decision>`

## 2. Decision
`<the decision, stated plainly>`

## 3. Consequences
`<positive + negative results; what becomes easier/harder; new invariants>`

## 4. Alternatives Considered
`<option A — pros/cons>` · `<option B — pros/cons>` · `<why rejected>`

## 5. Affected Docs / Contracts
`<docs/specs/@quad/core types updated in the same PR>`

## 6. Migration / Rollout Notes
`<expand/contract? feature flag? backward compatibility; → DEPLOYMENT.md>`

## 7. Follow-Up Actions
`<tasks/milestones spawned; open questions; revisit triggers>`

## Document Control
- **Path:** `docs/adr/<NNNN>-<slug>.md` · **Acceptance:** ☑ status set ☑ context/decision/consequences ☑ alternatives ☑ affected docs listed ☑ referenced by the doc it governs.
